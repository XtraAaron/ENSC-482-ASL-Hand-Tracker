Need to download eurofilter and mediapipe to run

First do this: .\aslenv\Scripts\Activate.ps1"
Starts virtual enviroment, i did this all in aslenv so this is what its designed for, should work for other virtual envs?
eurofilter: pip install OneEuroFilter
mediapipe: pip install mediapipe

If u want to run via .bat, run .\run_all.bat, then run the blender script. Make sure hand is in neutral position, (and fingers are together, so like a high five pos)


Put other stuff abt it here ig